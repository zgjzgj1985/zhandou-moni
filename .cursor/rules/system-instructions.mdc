---
description: "全局系统指令，定义 AI 行为规范"
alwaysApply: true
---

# 全局系统指令



## 推理过程规范

**核心原则**：所有推理过程必须使用中文进行思维和表述，不得混入英文、日文等其他语言。代码标识符（变量名、函数名、文件名等实际代码）不受此限制。

具体要求：

- 分析问题时使用中文连贯表达
- 设计方案时使用中文逻辑推导
- 解释代码时使用中文清晰阐述
- 过渡语使用中文（如"让我看看"、"首先"、"接下来"等），禁止使用"Let me..."、"Here's..."、"Looking at..."等英文过渡语
- 当引用代码中的英文标识符时，仅保留标识符本身，不将英文解释性词语混入推理文本

**违规认定**：推理过程中出现英文过渡语、英文解释性词语（代码标识符除外）视为违反本规则。

## 临时文件管理规范

**核心原则**：临时文件必须统一管理，不得污染项目根目录或其他业务目录。

管理要求：

- **统一存放位置**：所有在解决问题过程中产生的临时文件（如调试输出、批处理脚本、进度记录、中间数据等）必须放置到项目根目录下的 `temp` 目录中
- **禁止污染项目根目录**：不得在项目根目录或其他业务目录中创建临时文件
- **目录自动创建**：如果 `temp` 目录不存在，AI 应自动创建后再使用
- **定期清理提醒**：AI 可以在完成任务后提醒用户是否需要清理 `temp` 目录中的文件

**违规处理**：违反本规则视为行为错误，应立即停止并纠正。
