---
description: 强制 AI 只改动用户明确要求的内容，改动前需评估影响范围
alwaysApply: true
version: v1.0.0
lastUpdated: 2026-05-28
---

# 改动范围控制规范

## 核心原则

**严格按照用户要求执行，只改动需要改动的部分，不擅自添加、优化或重构用户未提及的内容。**

## 细则

### 1. 严格遵循用户指定范围

- 用户要求修改什么，就只修改什么
- 不因为"觉得可以更好"而额外改动
- 不因为"代码可以优化"而重构无关部分
- 不因为"发现潜在问题"而修改其他功能

### 2. 改动前评估影响范围

在修改任何代码之前，必须思考：

- **这个改动会影响哪些功能？**
- **这个改动会破坏现有逻辑吗？**
- **这个改动是否涉及共享代码或公共函数？**

如果改动涉及多个模块或影响范围不明确，应先确认。

### 3. 涉及范围外的改动必须确认

当用户要求的功能改动**必然会牵连**到用户未提及的部分时：

1. **先停下来**，不要直接改
2. **向用户说明**：改动需要涉及哪些额外部分
3. **等待确认**：只有用户同意后才执行

**需要主动确认的情况包括**：

- 修改共享函数/工具函数（会影响调用它的所有地方）
- 调整数据结构（影响相关的数据读写逻辑）
- 改动公共样式（影响多个页面或组件）
- 改动核心逻辑（影响依赖它的其他功能）
- 任何"看起来顺眼的顺手改动"

### 4. 避免过度优化

- 不要在修改 A 功能时"顺便"优化 B 功能的代码
- 不要因为旧代码"可以写得更好"而重构
- 用户没有要求改进的性能/代码风格，不要擅自优化

### 5. 保持最小改动原则

当有多种实现方式时：

- 优先选择**改动范围最小**的方案
- 优先选择**不影响现有逻辑**的方案
- 优先选择**可逆**的方案

## 正确示例

**用户说**：修改按钮颜色为红色

✅ 正确做法：只修改按钮颜色相关的 CSS/样式
❌ 错误做法：顺便修改按钮大小、圆角、阴影、字体等

**用户说**：添加一个新的计算函数

✅ 正确做法：只添加函数定义和必要的调用
❌ 错误做法：顺便重构整个模块的代码风格

## 违规处理

违反本规则视为行为错误。如果发现自己在做用户未要求的事，应立即停止并向用户确认。
