# 文件操作与编码安全规范

## 核心原则

**修改成功后立即停止**，不要画蛇添足。

## 细则

### 1. 修改验证优先

当你用 `StrReplace` 或 `Write` 成功修改文件后：
- 用 `Grep` 验证关键内容是否正确
- **不要**在未验证前执行任何编码修复命令
- **不要**反复读取同一文件检查"是否乱码"

### 2. Read工具显示乱码 ≠ 文件实际乱码

Read工具在渲染某些编码的文件时可能显示为乱码，但文件本身可能是正常的。

**正确做法**：
- 用 `Grep` 搜索关键词验证实际内容
- 如果 grep 能找到正确内容，说明文件没问题

**错误做法**：
- 在未验证前执行 PowerShell 编码修复命令
- 反复读写文件试图"修复"

### 3. 破坏性操作前必须确认

以下操作具有破坏性，执行前必须明确告知用户并获得确认：
- 删除文件
- 覆盖整个文件内容
- 修改编码可能损坏文件

### 4. 简单修改一步到位

对于简单的单点修改（如添加一行描述）：
- 完成后用 `Grep` 验证
- 验证通过后直接告知用户完成
- 不要做额外检查或"优化"

### 5. 编码问题处理流程

如果用户报告"乱码"问题，按以下顺序处理：

1. **第一步**：用 `Grep` 搜索关键词，确认内容是否正确
2. **如果 grep 结果正确**：告知用户文件正常，可能是 Read 工具渲染问题
3. **如果 grep 确认有问题**：才考虑修复编码，且只修复必要的文件

### 6. 避免连续写入冲突

- 避免在短时间内对同一文件进行多次连续写入
- 每次写入后优先验证，再决定是否继续

## 违规处理

违反本规则视为行为错误，会浪费大量 token 并可能损坏文件。发现错误后应立即停止并纠正。
